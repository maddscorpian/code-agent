OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=deepseek-coder-v2
OLLAMA_EMBED_MODEL=nomic-embed-text
CHROMA_PATH=./vector_db
DIGESTS_PATH=./digests
PROJECTS_CONFIG=./projects.yaml
API_PORT=8765
LOG_LEVEL=INFO

# Optional: point tiktoken to a local cache directory to avoid SSL download.
# Rename the downloaded cl100k_base.tiktoken file to the SHA-256 hash of its URL:
#   26b9c229141b3d34dcac6d3728f94f1e40abb67ef4a84ca1351abc0a20e6b701
# Place that file inside the directory set here, then uncomment the line below.
# TIKTOKEN_CACHE_DIR=./tiktoken_cache
